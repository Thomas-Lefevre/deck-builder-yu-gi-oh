{% set orderBy = app.request.query.get("order") %}
{% if orderBy is empty %}
	{% set orderBy = "nom" %}
{% endif %}
{% for card in cards | sort((a,b) => attribute(a,orderBy) <=> attribute(b,orderBy) )%}
	<div class="card">
		<a href="{{ path('insert_card_in_deck' , {'id_card' :card.id ,'id_deck' : deck.id }) }}">
			<img src="{{ asset(card.img)}}" alt="{{card.nom}}"class="card-img-top">
		</a>	
	</div>
{% endfor %}
