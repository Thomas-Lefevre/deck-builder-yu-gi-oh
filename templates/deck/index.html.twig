{% extends 'base.html.twig' %}

{% block stylesheets %}
	<link rel="stylesheet" href="{{ asset('css/indexDeck.css') }}">
{% endblock stylesheets %}


{% block title %}Deck index{% endblock %}

{% block body %}
<h1>Deck index</h1>
<div class="container-fluid p-0">
    <div class="row p-5 m-0">
          {% for deck in deck %}
        {# {% if loop.index <= 12 %}  #}
        <div class="col-md-6 col-lg-2 p-3">
            <a class="text-dark" href="{{ path('deck_show', {'id': deck.id}) }}">
                <div class="card">
                    <img class="card-img-top" src="{{asset(deck.img)}}" alt="image deck">
                    <div class="card-body">
                        <h5 class="card-title">{{ deck.nom }}</h5>
                        <p class="card-text">Author: {{ deck.auteur }}</p>
                        <p class="card-text">Date: {{ deck.datePost ? deck.datePost|date('Y-m-d') : '' }}</p>
                        <p class="card-text">Note: {{ deck.note }}</p>
                    </div>
                </div>
            </a>
        </div>
        {# {% endif %} #}
        {% endfor %} 
        {# <div class="row collapse" id="collapseExample">
            {% for deck in decks %}
            {% if loop.index > 12  %}
            <div class="col-md-6 col-lg-2 p-3">
                <a class="text-dark" href="{{ path('deck_show', {'id': deck.id}) }}">
                    <div class="card">
                        <img class="card-img-top" src="{{asset(deck.img)}}" alt="image deck">
                        <div class="card-body">
                            <h5 class="card-title">{{ deck.nom }}</h5>
                            <p class="card-text">{{ deck.auteur }}</p>
                            <p class="card-text">{{ deck.datePost ? deck.datePost|date('Y-m-d') : '' }}</p>
                            <p class="card-text">{{ deck.note }}</p>
                        </div>
                    </div>
                </a>
            </div>
            {% endif %}
            {% endfor %} #}
        {# </div>
        <div class="row">
            <div class="col-12 text-center">
                <button class="btn btn-secondary" type="button" data-toggle="collapse" data-target="#collapseExample"
                    aria-expanded="false" aria-controls="collapseExample">
                    See all
                </button>
            </div>
        </div> #}
        {# <a href="{{ path('deck_new') }}">Create new</a> #}
        {{ knp_pagination_render(deck) }}
    </div>
</div>
{% endblock %}